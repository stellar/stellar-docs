---
title: Configurando
sidebar_position: 20
---

# Configurando

## Pasos para configurar Galexie

1. **Copia la configuración de ejemplo**

   Comienza con el archivo de ejemplo proporcionado, [`config.example.toml`](https://github.com/stellar/go/blob/master/services/galexie/config.example.toml).

2. **Renombra y actualiza la configuración**

   Renombra el archivo a `config.toml` y ajusta la configuración según sea necesario.

   - **Las configuraciones clave incluyen:**

     - **Bucket de Google Cloud Storage (GCS)**

       Especifica el bucket de GCS donde Galexie exportará los datos del ledger de Stellar. Actualiza `destination_bucket_path` con la ruta completa de tu bucket de GCS, incluyendo subrutas si es necesario.

       ```toml
       destination_bucket_path = "stellar-network-data/testnet"
       ```

     - **Red de Stellar**

       Establece la red de Stellar que se utilizará para crear el lago de datos.

       ```toml
       network = "testnet"
       ```

     - **Organización de datos (opcional)**

       Configura cómo se organizarán los datos exportados en el bucket de GCS. El ejemplo siguiente añade 1 ledger por archivo y los organiza en un directorio de 64000 archivos.

       ```toml
        # Number of ledgers stored in each file
        ledgers_per_file = 1

        # Number of files per partition/directory
        files_per_partition = 64000
       ```
