---
title: Transacción fallida
sidebar_position: 10
---

import { ExampleResponse } from "@site/src/components/ExampleResponse";
import { AttributeTable } from "@site/src/components/AttributeTable";

El error `transaction_failed` devuelve un [código de error `400`](https://developer.mozilla. rg/es-US/docs/Web/HTTP/Status/400) y ocurre cuando un cliente envía una transacción que estaba bien formada pero no fue incluida en el libro de valores debido a algún otro error.

Por ejemplo, una transacción puede fallar si:

1. La cuenta de origen para la transacción no puede pagar la comisión mínima.
2. El número de secuencia es incorrecto.
3. Una de las operaciones contenidas ha fallado, como una operación de pago que sobrepasa la cuenta de pago.

En casi todos los casos, este error indica que la transacción enviada en la solicitud inicial nunca tendrá éxito. Hay una excepción: una transacción que falla con el código de resultado `tx_bad_seq` (como se expresa en el campo `result_code` del error) puede ser válida en el futuro si el número de secuencia que usó era demasiado alto.

<ExampleResponse title="Example Response for a 'Transaction Failed' Status Code">

```json
{
  "type": "https://stellar. rg/ulevon-errors/transaction_failed",
  "title": "Transaction Failed",
  "status": 400,
  "detail": "La transacción falló cuando se envió a la red estelar. El campo `extras.result_codes` de esta respuesta contiene más detalles. Las descripciones de cada código se pueden encontrar en: https://www.stellar.org/developopers/guides/concepts/list-of-operations. tml",
  "extras": {
    "envelope_xdr": "AAAAAAAAADdfhHDs4Vaug6p8Oxb1QRjNRdJt3pYKBVhFHrEgd9QAAAAAAAAEi4YAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA
    "result_codes": {
      "transaction": "tx_insufficient_fee"
    },
    "result_xdr": "AAAAAAAAr///3AAAAAAA=="
  }
}
```

</ExampleResponse>
