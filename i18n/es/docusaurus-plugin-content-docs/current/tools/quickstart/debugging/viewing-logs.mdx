---
title: Ver registros
description: Ver registros dentro del contenedor Quickstart
sidebar_label: Ver registros
sidebar_position: 20
---

# Ver registros

Puedes ver registros iniciando un nuevo shell interactivo dentro del contenedor en ejecución:

```sh
docker exec -it stellar /bin/bash
```

El comando anterior asume que iniciaste tu contenedor con el nombre `stellar`. Reemplaza ese nombre con el que elegiste, si es diferente. Cuando se ejecute, abrirá un shell interactivo que se ejecuta como root dentro del contenedor.

Los registros se pueden encontrar dentro del contenedor en la ruta `/var/log/supervisor/`. Se mantiene un archivo tanto para el **_stdout_** como para el **_stderr_** de los procesos gestionados por [supervisord](https://supervisord.org/index.html). Además, puedes usar el comando `tail` proporcionado por supervisord.

Alternativamente, para mostrar todos los registros en la salida del contenedor para todos los servicios, agrega la opción `--logs`.
