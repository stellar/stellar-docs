---
title: Pagos y activos
hide_table_of_contents: true
description: Envía XLM, stellar classic o un activo soroban usando la CLI de Stellar
custom_edit_url: https://github.com/stellar/stellar-cli/edit/main/cookbook/payments-and-assets.mdx
---

Para enviar pagos y trabajar con activos usando la CLI de Stellar, sigue estos pasos:

1. Configura tu red preferida. Para esta guía, usaremos `testnet`. Una lista de redes disponibles puede encontrarse [aquí](../../../networks/README.mdx)

```bash
stellar network use testnet
```

2. Financia las cuentas:

```bash
stellar keys generate alice
```

```bash
stellar keys fund alice
```

```bash
stellar keys generate bob
```

```bash
stellar keys fund bob
```

3. Obtén el ID del contrato del activo stellar:

```bash
stellar contract id asset --asset native
```

4. Obtén la clave pública de Bob:

```bash
stellar keys address bob
```

5. Envía 100 XLM de Alice a Bob:

```bash
stellar keys use alice
```

```bash
stellar contract invoke --id <ASSET_CONTRACT_ID> -- transfer --to bob --from alice --amount 100
```

6. Consulta el saldo de la cuenta:

```bash
stellar contract invoke --id <ASSET_CONTRACT_ID> -- balance --id bob
```

Para más información sobre las funciones disponibles para el contrato de activo Stellar, consulta el [código de la interfaz de token](../../../tokens/token-interface.mdx#code)
