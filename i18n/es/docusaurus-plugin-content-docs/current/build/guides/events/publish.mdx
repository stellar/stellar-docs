---
title: Publicar eventos de un contrato de Rust
hide_table_of_contents: true
description: Publicar eventos de un contrato de Rust.
---

Un evento puede contener hasta 4 temas, junto con los datos que está publicando. Los `data` pueden ser cualquier valor o tipo que quieras. Sin embargo, los temas no deben contener:

- `Vec`
- Mapa
- `Bytes` o `BytesN` más de 32 bytes
- `contracttype`

```rust
// This function does nothing beside publishing events. Topics we are using are
// some `u32` integers for the sake of simplicity here.
pub fn events_function(env: Env) {
    // A symbol will be our `data` we want published
    my_data = Symbol::new(&env, "data_to_publish");

    // an event with 0 topics
    env.events().publish((), my_data.clone());

    // an event with 1 topic (Notice the extra comma after the topic in the
    // tuple? That comma is required in Rust to make a one-element tuple)
    env.events().publish((1u32,), my_data.clone());

    // an event with 2 topics
    env.events().publish((1u32, 2u32), my_data.clone());

    // an event with 3 topics
    env.events().publish((1u32, 2u32, 3u32), my_data.clone());

    // an event with 4 topics
    env.events().publish((1u32, 2u32, 3u32, 4u32), my_data.clone());
}
```

Se puede encontrar un ejemplo más realista en la forma en que funciona la [interfaz de tokens]. Por ejemplo, la interfaz requiere que se publique un evento cada vez que se invoca la función `transfer`, con la siguiente información:

```rust
transferencia de pub fn (env: Env, de: Dirección, a: Dirección, amount: i128) {
    // transferencia lógica omitida aquí
    env. venta(). ublish(
        (symbol_short! "transferencia"), para,
        monto
    );
}
```

[interfaz de token]: ../../../tokens/token-interface.mdx
