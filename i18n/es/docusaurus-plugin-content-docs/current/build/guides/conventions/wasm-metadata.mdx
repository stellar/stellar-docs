---
title: Escribir metadatos para tu contrato
hide_table_of_contents: true
description: Usa el contractmeta! macro en Rust SDK para escribir metadatos en contratos Wasm
---

<head>
  <title>Escribir metadatos estructurados.</title>
  <meta charSet="utf-8" />
  <meta property="og:title" content="Escribir metadatos para tu contrato." />
  <meta
    property="og:description"
    content="Usa un macro proporcionado en el Rust SDK para escribir metadatos estructurados y usa las herramientas disponibles para leer y mostrar esta información a los usuarios."
  />
</head>

El macro [`contractmeta!`](https://docs.rs/soroban-sdk/22.0.4/soroban_sdk/macro.contractmeta.html) proporcionado en el Rust SDK permite a los usuarios escribir dos cadenas - una `key` y una `val` - dentro de un objeto XDR serializado `SCMetaEntry::SCMetaV0` en la sección personalizada de los contratos Wasm. El nombre de la sección para estos metadatos es `contractmetav0`. Los desarrolladores pueden utilizar este macro para escribir metadatos, y las herramientas pueden luego leer y mostrar esta información a los usuarios.

El [ejemplo de fondos de liquidez](https://github.com/stellar/soroban-examples/blob/v22.0.1/liquidity_pool/src/lib.rs#L152-L155) proporciona una demostración clara de cómo usar el macro `contractmeta!`:

```rust
// Metadata that is added on to the Wasm custom section
contractmeta!(
    key = "Description",
    val = "Constant product AMM with a .3% swap fee"
);

pub trait LiquidityPoolTrait {...
```
