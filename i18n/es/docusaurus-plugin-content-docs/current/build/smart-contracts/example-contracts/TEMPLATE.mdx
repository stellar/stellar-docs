---
title: Plantilla Tutorial
description: Una descripción del tutorial que se está escribiendo.
draft: true
sidebar_position: 1000
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { getPlatform } from "@site/src/helpers/getPlatform";

Nota rápida sobre lo que este [ejemplo demuestra]. Tal vez también se basa en algún [otro ejemplo].

[![Abrir en Gitpod](https://gitpod.io/button/open-in-gitpod.svg)][oigp]

:::tip

Coloque cada definición de enlace en la parte inferior de la sección en la que se utiliza primero.

:::

[oigp]: https://gitpod.io/#https://github.com/stellar/soroban-examples/tree/v20.0.0
[ejemplo demuestra]: https://github.com/stellar/soroban-examples/tree/v20.0.0/hello_world
[otro ejemplo]: ../iniciando/setup.mdx

## Ejecutar el ejemplo

Primero pasa por el proceso [Setup] para configurar tu entorno de desarrollo, luego clona la etiqueta `v20.0.0` del repositorio `soroban-examples`:

```sh
git clon -b v20.0.0 https://github.com/stellar/soroban-examples
```

O, omita la configuración del entorno de desarrollo y abre este ejemplo en [Gitpod][oigp].

Para ejecutar las pruebas para el ejemplo, navega al directorio `hello_world`, y usa `cargo test`.

```sh
cd hello_world
prueba de carga
```

Deberías ver la salida:

```sh
ejecutando 1 prueba
test test::test ... ok
```

[configuración]: ../iniciando/setup.mdx

## Código

```rust title="hello_world/src/lib.rs"
#![no_std]
use soroban_sdk::{contractimpl, vec, Env, Symbol, Vec};

pub struct HelloContract;

#[contractimpl]
impl HelloContract {
    pub fn hola(env: Env, to: Symbol) -> Vec<Symbol> {
        vec! &env, Symbol::short("Hola"), a]
    }
}

prueba de mod
```

Ref: https://github.com/stellar/soroban-examples/tree/v20.0.0/hello_world

## Cómo funciona

Esta es la parte escrita de cada guía. Puede llamar a cada cosa exclusiva de este contrato, a veces haciendo referencia a otros conceptos importantes de otros contratos de ejemplo.

### Conceptos principales

Podría añadir subtítulos para resaltar aún más bits o conceptos importantes a conocer. La guía de ejemplo de `events` tiene los siguientes encabezados `h3`:

- Temas del evento
- Datos del evento
- Publicación

Debajo de cada uno de esos títulos hay una breve discusión sobre cómo este concepto se relaciona con el código de contrato de ejemplo.

## Tests

Abre el archivo `/hello_world/src/test.rs` para seguirlo.

```rust title="hello_world/src/test.rs"
#![cfg(test)]

use super::*;
use soroban_sdk::{vec, Env, Symbol};

#[test]
fn test() {
    let env = Env::default();
    let contract_id = env. egister_contract(None, HelloContract);
    let client = HelloContractClient::new(&env, &contract_id);

    let words = cliente. ello(&Symbol::short("Dev"));
    assert_eq!
        palabras,
        vec! &env, Symbol::short("Hola"), Symbol::short("Dev"),]
    );
}
```

Puedes describir lo que está pasando en la prueba anterior aquí. Es probable que algunas de las cosas sean las mismas de una guía a otra. Parece que, en particular, el siguiente fragmento se comparte entre algunos de ellos:

{/_ BEGIN fragmento compartido _/}

En cualquier prueba lo primero que siempre se requiere es un `Env`, que es el entorno Soroban en el que se ejecutará el contrato.

```rust
let env = Env::default();
```

El contrato se registra con el entorno utilizando el tipo de contrato.

```rust
let contract_id = env.register_contract(None, IncrementContract);
```

Todas las funciones públicas dentro de un bloque `impl` que está anotado con el atributo `#[contractimpl]` tienen una función correspondiente generada en un tipo de cliente generado. El tipo de cliente se nombrará igual que el tipo de contrato con `Client` adjunto. Por ejemplo, en nuestro contrato el tipo de contrato es `Contract`, y el cliente se llama `ContractClient`.

{/_ FIN trozo compartido _/}

Entonces usted puede describir las complejidades de lo que su prueba de contrato hace de forma única.

## Construir el contrato

Para construir el contrato, utiliza el comando `cargo build`.

```sh
carga build --target wasm32-unknown-unknown --release
```

Un archivo `.wasm` debe ser enviado en el directorio `target`:

```
objetivo/wasm32-unknown-unknown/release/soroban_hola_world_contract.wasm
```

## Ejecutar el contrato

Si tienes instalado [`stellar-cli`], puedes invocar las funciones del contrato utilizándolo.

<Tabs groupId="platform" defaultValue={getPlatform()}>

<TabItem value="unix" label="macOS/Linux">

```sh
contrato estelar invoca \
    --wasm target/wasm32-unknown-unknown/release/soroban_hello_world_contract.wasm \
    --id 1 \
    -- \
    hola \
    --to Soroban
```

</TabItem>

<TabItem value="windows" label="Windows (PowerShell)">

```powershell
contrato estelar invoca `
    --wasm target/wasm32-unknown-unknown/release/soroban_hello_world_contract.wasm `
    --id 1 `
    -- `
    hola `
    --to Soroban
```

</TabItem>

</Tabs>

[`stellar-cli`]: ../getting-started/setup.mdx#install-the-stellar-cli

## Leyendo más

Si tienes más enlaces para compartir o compartir conocimientos de fondo a los que puedes enlazar, este es el lugar para compartirlos. O, tal vez quiera señalar cómo este ejemplo es similar o no en comparación con otros ejemplos.
