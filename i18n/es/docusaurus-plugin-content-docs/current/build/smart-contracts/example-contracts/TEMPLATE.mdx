---
title: Plantilla de Tutorial
description: Una descripción del tutorial que se está escribiendo.
draft: true
sidebar_position: 1000
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { getPlatform } from "@site/src/helpers/getPlatform";

Nota rápida sobre lo que este [ejemplo demuestra](https://github.com/stellar/soroban-examples/tree/v23.0.0/hello_world). Quizás también se base en algún [otro ejemplo](../getting-started/setup.mdx).

[![Abrir en Codespaces](https://github.com/codespaces/badge.svg)](https://github.com/codespaces/new?repo=stellar/soroban-examples&editor=web)

[![Abrir en Codeanywhere](https://codeanywhere.com/img/open-in-codeanywhere-btn.svg)](https://app.codeanywhere.com/#https://github.com/stellar/soroban-examples)

:::tip

Coloca cada definición de enlace al final de la sección en la que se utiliza por primera vez.

:::


## Ejecutar el Ejemplo

Primero realiza el proceso de [Configuración](../getting-started/setup.mdx) para preparar tu entorno de desarrollo, luego clona la etiqueta `v23.0.0` del repositorio `soroban-examples`:

```sh
git clone -b v23.0.0 https://github.com/stellar/soroban-examples
```

O, omite la configuración del entorno de desarrollo y abre este ejemplo en [GitHub Codespaces](https://github.com/codespaces/new?repo=stellar/soroban-examples&editor=web) o en [Code Anywhere](https://app.codeanywhere.com/#https://github.com/stellar/soroban-examples)

Para ejecutar las pruebas del ejemplo, navega al directorio `hello_world`, y usa `cargo test`.

```sh
cd hello_world
cargo test
```

Deberías ver la salida:

```sh
running 1 test
test test::test ... ok
```


## Código

```rust title="hello_world/src/lib.rs"
#![no_std]
use soroban_sdk::{contractimpl, vec, Env, Symbol, Vec};

pub struct HelloContract;

#[contractimpl]
impl HelloContract {
    pub fn hello(env: Env, to: Symbol) -> Vec<Symbol> {
        vec![&env, Symbol::short("Hello"), to]
    }
}

mod test;
```

Referencia: https://github.com/stellar/soroban-examples/tree/v23.0.0/hello_world

## Cómo Funciona

Esta es la parte escrita de cada guía. Puedes destacar cada cosa única de este contrato, a veces haciendo referencia a otros conceptos importantes de otros contratos de ejemplo, también.

### Conceptos Principales

Podrías agregar subtítulos para resaltar aún más partes o conceptos importantes que conocer. La guía de ejemplo `events` tiene los siguientes encabezados `h3`:

- Temas de Eventos
- Datos de Eventos
- Publicación

Debajo de cada uno de esos encabezados hay una breve discusión sobre cómo ese concepto se relaciona con el código de contrato de ejemplo.

## Pruebas

Abre el archivo [`/hello_world/src/test.rs`](https://github.com/stellar/soroban-examples/tree/v23.0.0/hello_world/src/test.rs) para seguir el ejemplo.

```rust title="hello_world/src/test.rs"
#[test]
fn test() {
    let env = Env::default();
    let contract_id = env.register(HelloContract, ());
    let client = HelloContractClient::new(&env, &contract_id);

    let words = client.hello(&String::from_str(&env, "Dev"));
    assert_eq!(
        words,
        vec![
            &env,
            String::from_str(&env, "Hello"),
            String::from_str(&env, "Dev"),
        ]
    );
}
```

Puedes describir lo que está sucediendo en la prueba anterior aquí. Algunas cosas probablemente serán las mismas de una guía a otra. Parece que, en particular, el siguiente fragmento se comparte entre varios de ellos:

{/* INICIO del fragmento compartido */}

En cualquier prueba, lo primero que siempre se requiere es un `Env`, que es el entorno de Soroban en el que se ejecutará el contrato.

```rust
let env = Env::default();
```

El contrato se registra con el entorno utilizando el tipo de contrato.

```rust
let contract_id = env.register(IncrementContract, ());
```

Todas las funciones públicas dentro de un bloque `impl` que está anotado con el atributo `#[contractimpl]` tienen una función correspondiente generada en un tipo de cliente generado. El tipo de cliente se nombrará igual que el tipo de contrato con `Client` añadido. Por ejemplo, en nuestro contrato el tipo de contrato es `Contract`, y el cliente se llama `ContractClient`.

{/* FIN del fragmento compartido */}

Luego puedes describir las complejidades de lo que hace tu prueba de contrato de manera única.

## Crear el Contrato

Para crear el contrato, utiliza el comando `cargo build`.

```sh
cargo build --target wasm32v1-none --release
```

Un archivo `.wasm` debería generarse en el directorio `target`:

```
target/wasm32v1-none/release/soroban_hello_world_contract.wasm
```

## Ejecutar el Contrato

Si tienes [`stellar-cli`](../getting-started/setup.mdx#install-the-stellar-cli) instalado, puedes invocar funciones del contrato usando esto.

<Tabs groupId="platform" defaultValue={getPlatform()}>
<TabItem value="unix" label="macOS/Linux">

```sh
stellar contract invoke \
    --wasm target/wasm32v1-none/release/soroban_hello_world_contract.wasm \
    --id 1 \
    -- \
    hello \
    --to Soroban
```

</TabItem>
<TabItem value="windows" label="Windows (PowerShell)">

```powershell
stellar contract invoke `
    --wasm target/wasm32v1-none/release/soroban_hello_world_contract.wasm `
    --id 1 `
    -- `
    hello `
    --to Soroban
```

</TabItem>
</Tabs>


## Lectura Adicional

Si tienes algunos enlaces adicionales para compartir o conocimiento de fondo al que puedes vincular, este es el lugar para compartirlo. O, quizás quieras señalar cuán similar o diferente es este ejemplo en comparación con otros ejemplos.
