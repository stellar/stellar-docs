---
title: El Primer Paso para Crear una Aplicación de Billetera en Stellar con el Wallet SDK
sidebar_position: 20
sidebar_label: Comenzar
description: Aprende a crear una aplicación de billetera en la red Stellar con el Wallet SDK. Explora características clave, pasos de configuración, fundamentos de Stellar y conceptos básicos de anchor.
---

# Comenzar

import { LanguageSpecific } from "@site/src/components/LanguageSpecific";
import { WalletCodeExample } from "@site/src/components/WalletCodeExample";
import Header from "./component/header.mdx";
import KtInstall from "./component/kt/install.mdx";
import TsInstall from "./component/ts/install.mdx";
import DartInstall from "./component/dart/install.mdx";
import SwiftInstall from "./component/swift/install.mdx";
import KtHttpConfig from "./component/kt/httpConfig.mdx";
import KtConfigClient from "./component/kt/configClient.mdx";
import TsConfigClient from "./component/ts/configClient.mdx";
import DartConfigClient from "./component/dart/configClient.mdx";
import AllowHttpInfo from "./component/ts/allowHttpInfo.mdx";

<Header />

## Instalación

Primero, necesitas agregar la dependencia del SDK a tu proyecto.

<LanguageSpecific
  kt={<KtInstall />}
  ts={<TsInstall />}
  flutter={<DartInstall />}
  swift={<SwiftInstall />}
/>

## Trabajando con el SDK

Comencemos con la clase principal que proporciona toda la funcionalidad del SDK. Se recomienda tener un objeto billetera singleton compartido en toda la aplicación. Crear una billetera con una configuración predeterminada conectada al Testnet de Stellar es simple:

<WalletCodeExample>

```kotlin
val wallet = Wallet(StellarConfiguration.Testnet)
```

```ts
let wallet = walletSdk.Wallet.TestNet();
```

```dart
var wallet = Wallet.testNet;
```

```swift
let wallet = Wallet.testNet
```

</WalletCodeExample>

La instancia de la billetera se puede configurar aún más. Por ejemplo, para conectarte a la red pública:

<WalletCodeExample>

```kotlin
val walletTestnet = Wallet(StellarConfiguration(Network.TESTNET, "https://horizon-testnet.stellar.org"))
```

```typescript
let wallet = new Wallet({
  stellarConfiguration: StellarConfiguration.MainNet(),
});
```

```dart
var wallet = Wallet(StellarConfiguration.publicNet);
```

```swift
let wallet = Wallet(stellarConfig: StellarConfig.publicNet)
```

</WalletCodeExample>

<LanguageSpecific kt={<KtHttpConfig />} />

<LanguageSpecific
  kt={<KtConfigClient />}
  ts={<TsConfigClient />}
  flutter={<DartConfigClient />}
/>

## Conceptos básicos de Stellar

El SDK de la billetera proporciona algunas funcionalidades adicionales además del [SDK Horizon](/docs/tools/sdks) existente. Para interactuar con la red Stellar, el SDK de la billetera solo cubre los conceptos básicos utilizados en un flujo típico de billetera. Para casos de uso más avanzados, se debe utilizar el [SDK Horizon](/docs/tools/sdks) subyacente en su lugar.

Para interactuar con la instancia de Horizon configurada en los pasos anteriores, simplemente haz:

<WalletCodeExample>

```kotlin
val stellar = wallet.stellar()
```

```typescript
const stellar = wallet.stellar();
```

```dart
var stellar = wallet.stellar();
```

```swift
let stellar = wallet.stellar
```

</WalletCodeExample>

Este ejemplo creará una clase Stellar que gestiona la conexión al servicio de Horizon.

:::note

La configuración por defecto se conecta a la instancia pública de Horizon de Stellar. Puedes cambiar este comportamiento como se describe [arriba](#working-with-the-sdk).

:::

Puedes leer más sobre cómo trabajar con la red Stellar en la [sección respectiva](./stellar.mdx).

## Conceptos básicos de anchor

El uso principal del SDK es proporcionar una forma sencilla de conectarse a los anchors a través de un conjunto de protocolos conocidos como SEPs. Veamos cómo conectar al anchor de prueba de Stellar:

<WalletCodeExample>

```kotlin
val anchor = wallet.anchor("https://testanchor.stellar.org")
```

```typescript
let anchor = wallet.anchor({ homeDomain: "testanchor.stellar.org" });
```

```dart
var anchor = wallet.anchor("testanchor.stellar.org")
```

```swift
let anchor = wallet.anchor(homeDomain: "testanchor.stellar.org")
```

</WalletCodeExample>

<LanguageSpecific ts={<AllowHttpInfo />} />

Y la interacción más básica de recuperación de un [SEP-1](https://github.com/stellar/stellar-protocol/blob/master/ecosystem/sep-0001.md): Archivo de información de Stellar:

<WalletCodeExample>

```kotlin
suspend fun anchorToml(): TomlInfo {
  return anchor.getInfo()
}
```

```typescript
let resp = await anchor.sep1();
```

```dart
var resp = await anchor.sep1();
```

```swift
let resp = try await anchor.sep1
```

</WalletCodeExample>

A continuación se pueden encontrar todos los SEPs que actualmente admite la clase anchor:

- [SEP-1](https://github.com/stellar/stellar-protocol/blob/master/ecosystem/sep-0001.md): Archivo de información de Stellar como se mostró arriba
- [SEP-10](https://github.com/stellar/stellar-protocol/blob/master/ecosystem/sep-0010.md): explorado en la sección [Autenticación Stellar](/docs/build/apps/wallet/sep10)
- [SEP-24](https://github.com/stellar/stellar-protocol/blob/master/ecosystem/sep-0024.md): explorado en la sección [Depósito y Retirada Alojados](/docs/build/apps/wallet/sep24)
- [SEP-6](https://github.com/stellar/stellar-protocol/blob/master/ecosystem/sep-0006.md): explorado en la sección [Depósito y Retirada Programáticos](/docs/build/apps/wallet/sep6)
- [SEP-12](https://github.com/stellar/stellar-protocol/blob/master/ecosystem/sep-0012.md): explorado en la subsección [Proporcionando información KYC]
- [SEP-38](https://github.com/stellar/stellar-protocol/blob/master/ecosystem/sep-0038.md): explorado en la sección [Cotización](/docs/build/apps/wallet/sep38)

Además de los SEPs admitidos por la clase anchor, el SDK de la billetera también tiene soporte para el protocolo [SEP-7](https://github.com/stellar/stellar-protocol/blob/master/ecosystem/sep-0007.md) que se explora en la sección [Esquema de URI para facilitar la firma delegada](/docs/build/apps/wallet/sep7).
