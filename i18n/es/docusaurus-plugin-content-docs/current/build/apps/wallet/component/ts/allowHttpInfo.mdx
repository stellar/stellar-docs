import { CodeExample } from "@site/src/components/CodeExample";

:::info

Si el dominio principal de anclaje usa http, entonces necesitas establecer la bandera `allowHttp` al crear el ancla:

<CodeExample>

```typescript
let anchor = wallet.anchor({ homeDomain: "example.com", allowHttp: true });
```

</CodeExample>

Esto sólo se puede utilizar en Testnet.

:::
