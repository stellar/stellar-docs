{"parameters":[{"name":"X-Wallet-Id","in":"header","required":false,"description":"Optional. Selects the distribution account whose trustline is added. Omit it to act on the tenant's default account. An API key does not get that fallback: it resolves the account the same way other writes do, so a key scoped to exactly one account may omit the header and any other key must send it.\n","schema":{"type":"string"}}]}