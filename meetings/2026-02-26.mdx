---
title: "In-Memory Eviction Scan and Checked 256-bit Arithmetic Host Functions"
description: "Air date: 02/26/2026 In this protocol meeting we had two CAPs to discuss and that was CAP-81 and CAP-82. CAP-81: This CAP introduces a simpler and more efficient eviction scan proc"
authors:
  - garand-tyson
  - jay-geng
tags: [developer, CAP-81, CAP-82]
---

import YouTube from "@site/src/components/YouTube";

<YouTube ID="nLxODJal-xQ" />

### Key Points

- Faster State Eviction (CAP-81)
  - This CAP introduces a simpler and more efficient eviction scan process. Instead of scanning the BucketList, the new eviction scan just uses in-memory Soroban state. This simplifies the logic while reducing disk reads and speeding up eviction speed.
  - Entries are ordered by expiration ledger and key, with separate handling for temporary and persistent state. This allows the system to evict entries closer to their archival ledger while controlling limits on both entry count and bytes written to the archive.
- Checked `Int256` Arithmetic (CAP-82)
  - This CAP adds checked variants of the existing 256-bit integer arithmetic host functions. Unlike the existing functions which trap on overflow, the checked variants return `Void` on overflow, allowing contracts to handle arithmetic errors gracefully.
- CAP-81 & CAP-82: In-Memory Eviction Scans and Checked 256-bit Math
  - Simplified Eviction Scans and Overflow-Safe 256-bit Host Functions

### Resources

- [CAP-81](https://github.com/stellar/stellar-protocol/blob/master/core/cap-0081.md) - [Discussion](https://github.com/orgs/stellar/discussions/1868)
- [CAP-82](https://github.com/stellar/stellar-protocol/blob/master/core/cap-0082.md) - [Discussion](https://github.com/orgs/stellar/discussions/1834)

<details>
  <summary>Video Transcript</summary>

[00:00] Excellent. let's get into it. So, today I'll start with CAP 81 and then Jay is going to hop up here for some Jay is going to hop up here for some Jay is going to hop up here for some privacy stuff after that. cool. So, I just put a link to the CAP in the chat. just put a link to the CAP in the chat. just put a link to the CAP in the chat. but basically what this is is it's a bit of a a mix of a safety feature or a bit of a a mix of a safety feature or a bit of a a mix of a safety feature or a code hardening feature as well as an optimization with respect to the eviction scan. so the eviction scan is this process where currently how it works is every ledger we have this background thread that will scan some region of the bucket list and then if it region of the bucket list and then if it region of the bucket list and then if it finds anything that's expired it will essentially mark that this entry is evictable and then at le we will decide whether or not to actually evict that entry. now this

[01:00] actually evict that entry. now this process has a couple of issues. first it's very complex and this is where the bug in Protocol 23 actually was was in the eviction scan process. additionally it is expensive from a a resources standpoint for a couple of reasons. you have to have this background thread that's running in parallel to your ledger apply and you have to do a bunch of disk lookups. Now have to do a bunch of disk lookups. Now have to do a bunch of disk lookups. Now one of the sort of optimizations that was made in protocol 23 is that all non-evicted Soroban state is now stored in memory. And so what this CAP proposes is to basically leverage this in-memory state and instead of scanning the bucket list and doing all these dis lookups to look for a state that might be evictable just looking directly in the in-memory state instead. And so I think this is fairly straightforward. but basically what we're going to do is instead of evicting entries sort of based on their bucket list ordering which is sort of implementation specific to our database

[02:00] implementation specific to our database and very complex we have a much simpler ordering. we're essentially just going to order everything first by its live and tell ledger sequence and then by ledger keys a tiebreaker and we're going to have a separate lane for persistent entries and temporary entries at the end of every ledger. We will then just look at this list and evict everything that is evictable in order up until we either hit the max entries to evict or the maximum bytes that we can evict whichever limit occurs first. this should make the the code base a lot simpler because we don't have to do this background eviction scan thing. Everything is just blocking in one thread. Everything is in the mirror state. We're also getting rid of a bunch of disc reads. a couple of of a bunch of disc reads. a couple of of a bunch of disc reads. a couple of small details here. one sort of observable change is that today we have one sort of maximum entries to evict that applies both to temporary and

[03:00] that applies both to temporary and persistent. This breaks that into two separate sort of limits. We've observed that the temporary entries grow a lot faster than persistent entries. So this allows us to essentially crank up the amount of temporary entries we evict more than the persistent entries. The other thing too is that there's a maximum bytes to evict that applies to persistent and that's temporary. this is because temporary entries eviction is quite fast and simple. You just do a delete. However, for persistent entries, you actually have to do a write in writing them to the hot archive, which is why we need that limit to avoid writing too many bytes on electric. so it's kind of a overview. any any questions, concerns, anything folks want to talk about?

[04:00] I see a couple people typing, so I'll I'll let them finish their their thoughts. seems pretty straightforward. memory versus disk any trade-offs identified. one trade-off is that this ordering is a little bit more expensive to maintain. currently we don't have to maintain any ordering for eviction. but this does necessitate keeping a list of live ledger keys organized by or sorted by entry or expiration ledger and key. now the advantage to this is that you don't actually need to keep the entire list in memory. so basically you can identify say what entries will be evictable in the next five ledgers and you can just sort those entries. So you don't actually even if the entire state is a couple gigabytes large and has millions of entries, you don't have

[05:00] has millions of entries, you don't have to maintain the sorted order of all million entries. You can just sort of take a subset of those entries sorted. The reason we didn't do this before is because entries lived on disk and so sort of keeping this partially sorted list was actually very expensive because we do a bunch of disc reads in order to we do a bunch of disc reads in order to we do a bunch of disc reads in order to maintain your list. Because of inmemory state, that concern is no longer there, which is why this makes sense. And so the protocol will define the ordering for all entries, but the implementation will only have to keep a small subset of them sorted at any given time. time. time. row bear are there any edge cases where an entry could expire in X but there are too many entries to evict and so the entry remains live passed to the TL. yes that actually is what happens today. So today entries are not evicted immediately on their archival ledger. now that doesn't mean the entry stays live. so the semantics of live versus archival remain the same as they are today. So today say an entry is

[06:00] are today. So today say an entry is archived on ledger 10 but doesn't get evicted until ledger 15. If you try to access that on ledger 12 even though the entry hasn't been evicted yet it's still considered archived and so your transaction is still going to fail and still going to get the archived error code. So under this proposal it's the exact same. a a entry might not be evicted immediately but it's still considered archived. This actually helps a lot because with this change, we'll be able to evict entries much closer to their archival ledger. Whereas today, entries can actually live for a long time in the archive state before they get evicted. Cool. let's see. I see no more typing. I think those Thank you for those questions. I will going once. Going twice. Sold. Okay, cool. I guess that

[07:00] sold. Okay, cool. I guess that concludes the CAP 81 chat today. now I think Jay has some stuff to share. So I think Jay has some stuff to share. So I think Jay has some stuff to share. So if you want to hop on stage, thank y'all. Can you hear me? Okay. Yeah. hi everyone. yes talking about CAP 82 which is a checked 256 bit integer arithmetic host functions. yeah I think this CAP is fairly straightforward. We are adding eight new host functions that are almost identical to the eight existing host functions that performs integer arithmetics. those are U256 and I26. the reason we are adding them is that the the return the compute of

[08:00] the the return the compute of those host functions currently do not allow the contract cost to make fallable cost meaning that if if a if arithmic arithmic arithmic function overflows it will just panic instead of making it actionable. So, so contract will contract that will basically they can't act on potential values that overflow. This is important because for some libraries that ecosystem develop such as the the fixed point arithmetics functions. using the U256 as a building blocks require that you are able to check on overflow and be able to come back and do additional logic on top of so the

[09:00] do additional logic on top of so the current design that doesn't allow overflow to occur really hinders such development. So yeah, so these new host functions supports that. so in terms of semantics, it's very similar. I'm just taking one example U2 256 added checked add. so adding the two numbers if it overflows it will return a void variant of of the val. So return a void variant of of the val. So return a void variant of of the val. So originally the return value is a U256 val which can only be representing a U256 type. right now in the new new proposal this is a val more generic version val that could be both U26 or a void. any other types obviously will still get rejected like you can't be expecting a

[10:00] like you can't be expecting a vector return or add function. that's obvious. Yeah. So I think that's all that is to Yeah. So I think that's all that is to Yeah. So I think that's all that is to the semantics. so a couple of more details to to mention. I think the first obvious question is that I'm seeing a question. Wait. Okay. Yeah, exactly. You can map void into option type or or you can have your own custom logic to to you know to act on the void value instead of know to act on the void value instead of know to act on the void value instead of just panicking. Yeah. So, so yeah. So, the list here has Yeah. So, so yeah. So, the list here has Yeah. So, so yeah. So, the list here has eight functions. and originally the the list is 16. the the unchecked version has 16 and this one only has eight because some of the functions that are very trivial to add the checked version such as division a U256 integer division you just need to check

[11:00] integer division you just need to check the denominator to be non zero similar for I26 division shift operation so that saves us some additional footprint in the host to maintain and to implement. Yeah. So I think the so this is yeah Yeah. So I think the so this is yeah Yeah. So I think the so this is yeah nothing really is changing. There's no new added cost types. There's no new dependencies in terms of host that's really the same functionality but exposing it in a more friendly and generic way. Yeah. I guess the yeah the first I guess obvious question is why don't we just change the existing host functions changes the return type that's that is because we want to maintain the interface compatibility not just that new new function if we change it to val it's

[12:00] new function if we change it to val it's new function if we change it to val it's a broader type so any old contract that relies on U 256 will technically still work but there may be some flows cases such that a contract rely on the panicking behavior which now will be kind of silently removed. So this is could cause some like troubles for some contract. So that's why I wanted to to change the return type. We just have to add a new variant and then adapt have to add a new variant and then adapt have to add a new variant and then adapt that in the SDK.

[13:00] All right. if there's no more questions, I would hand it back to Ken or do you want me just Oh, I'll pause for a second.

[15:00] I don't see Lee of this call. oh, he I don't see Lee of this call. oh, he is. But if you want to, add any comments. Okay, can do this offline as well. Whatever. Up to you. All right. if no more questions, I guess casting is typing. All right. Thank you everyone. I'll see you next time.

</details>
